#ifndef MAJOR_H
#define MAJOR_H

unsigned int rotate_right(unsigned int input_number, int rotation_value);
unsigned int endian(unsigned int input_number);
unsigned long int parity(unsigned long int x);
int count_leading_zeros(unsigned int num);

#endif 